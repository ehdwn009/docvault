# dependencies
node_modules/

# build output
client/dist/
server/dist/

# persistent data (SQLite + binary files) — backup unit, never committed
data/
backups/

# env
.env
.env.local

# misc
*.log
.DS_Store
